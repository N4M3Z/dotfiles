#!/bin/bash
# Forwarder: this tool moved to the sd script tree (`sd claude resume`).
# Kept because saved tmux-resurrect layouts reference it by this name.
exec "$(dirname "$0")/sd" claude resume "$@"
