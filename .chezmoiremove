# Packs that moved into every runedeck repository (.dcg/packs/, skeleton
# SKEL-0009). A user copy would fire before the repository copy and defeat
# the per-rule relaxation in config.toml. The secret-read rule stays as a
# host pack (secrets.yaml), because it must hold outside those repositories.
# forge.yaml guarded a CLI that is no longer used; other.yaml is the host
# catch-all now.
.config/dcg/packs/toolpolicy.yaml
.config/dcg/packs/rtk.yaml
.config/dcg/packs/homebrew.yaml
.config/dcg/packs/forge.yaml
