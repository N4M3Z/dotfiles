# Handoff

No active mid-session work. Durable architecture and deploy instructions live in [../README.md](../README.md). Cross-repo session narrative for ongoing work lives in `~/Developer/N4M3Z/forge-provision/journal/`.

If you arrive here with uncommitted changes and no obvious context, run:

```sh
git status -s
git log --oneline -10
```

and reconstruct from the actual repo state rather than relying on stale prose in this file.
