#:schema https://docs.jj-vcs.dev/latest/config-schema.json

[user]
name = "Martin Zeman"
email = "N4M3Z@users.noreply.github.com"

[signing]
backend = "gpg"
# Commits and pushes stay unsigned: "drop" keeps jj from signing working-copy
# snapshots, and sign-on-push stays off so agent sessions never hit pinentry
# or the YubiKey mid-push. The owner's signature enters at release tags, and
# the signed tag vouches for the history beneath it.
behavior = "drop"
key = "0x29DD2145CE7A818929459B2649F08103D3DA399E"

[git]
sign-on-push = false

[aliases]
push = ["util", "exec", "--", "bash", "-c", "cd \"$(jj workspace root)\" && if [ -x .githooks/jj-push ]; then exec .githooks/jj-push \"$@\"; else exec jj git push \"$@\"; fi", "jj-push"]
# Move the nearest ancestor bookmark up to @- , ready to push.
tug = ["bookmark", "move", "--from", "closest_bookmark(@-)", "--to", "@-"]

[revset-aliases]
'closest_bookmark(to)' = 'heads(::to & bookmarks())'
